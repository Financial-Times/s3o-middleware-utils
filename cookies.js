/**
 * Shared cookie configuration and utilities
 *
 */

const USERNAME = 's3o_username'
const TOKEN = 's3o_token';
const DEFAULT_EXPIRY = 900000;

module.exports = {
	USERNAME,
	TOKEN,
	DEFAULT_EXPIRY,
}
